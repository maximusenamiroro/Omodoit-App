<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      Media access. Android 13 (API 33) replaced READ_EXTERNAL_STORAGE
      with per-type permissions and IGNORES the old one entirely — so
      without these two, picking a video or photo from the device
      library silently returns nothing on any modern phone. The app was
      only ever getting READ_EXTERNAL_STORAGE indirectly from a
      dependency's manifest, which does nothing on 33+.

      The legacy permission is kept but capped at API 32, where it is
      still the one that counts.
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!--
      tools:replace="android:allowBackup" below:
      react-native-compressor pulls in TAndroidLame, whose manifest sets
      allowBackup="true". The merger treats that as a conflict and fails
      the build rather than picking a winner, so we say explicitly that
      ours wins. Keep allowBackup="false" — this app stores auth tokens,
      and letting Android back them up would carry a signed-in session
      onto another device.
    -->
    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      tools:replace="android:allowBackup"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <!--
        Required for react-native-maps to work on Android at all —
        without this, the native map view can crash on init. iOS
        needs no key here since it uses Apple's built-in MapKit
        instead. Get a free key from Google Cloud Console:
        https://console.cloud.google.com -> enable "Maps SDK for
        Android" -> Credentials -> Create API Key.
      -->
      <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="AIzaSyA88wnNfw8Qp9S64bbu8VLE6x3mYMLdZNw" />
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>